const getTheTitles = function(books) {
    return books.map(item => item.title);
};

// Do not edit below this line
module.exports = getTheTitles;

// function getTitles(books){
//     console.log(books.map(item => item.title));
// };

// getTitles([
//     {title: "book1"},
//     {title: "book2"},
//     {title: "book3"},
// ]);
